# Copy to .env.local and fill in.
# Required for Layer B (model-graded conflict analysis). Without it, Ballast
# still runs — reports just contain the deterministic findings only.
ANTHROPIC_API_KEY=

# Optional: override the model used for conflict analysis.
# ANTHROPIC_MODEL=claude-sonnet-4-5
